# Taskly
Taskly is a lightweight web-based task manager built with Flask, SQLite, and Bootstrap. It lets users create tasks, assign categories, track progress, and view stats, focusing on a clean, intuitive interface while demonstrating CS50 concepts like backend, database, templating, and interactivity.
